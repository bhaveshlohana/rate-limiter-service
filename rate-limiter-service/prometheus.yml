global:
  scrape_interval: 5s

scrape_configs:
  - job_name: 'rate-limiter'
    static_configs:
      - targets: ['host.docker.internal:8080'] # Use host.docker.internal to access the app from Prometheus in Docker
    metrics_path: '/actuator/prometheus' # Ensure this matches your Spring Boot actuator endpoint